<nav class="guests-rsvp-nav">
  <ul class="nav nav-pills nav-fill">
    <li class="nav-item">
      <%= link_to '1. Fontos információk', guest_path(@guest), class: ['nav-link', active == 1 ? 'active' : ''] %>
    </li>
    <li class="nav-item">
      <% if active >= 2 %>
        <%= link_to '2. Akik veled jönnek', guest_plus_ones_path(@guest), class: ['nav-link', active == 2 ? 'active' : ''] %>
      <% else %>
        <a class="nav-link <%= active == 2 ? 'active' : '' %>">2. Plus Ones</a>
      <% end %>
    </li>
    <li class="nav-item">
      <% if active >= 3 %>
        <%= link_to '3. Megerősítés', confirm_guest_path(@guest), class: ['nav-link', active == 3 ? 'active' : ''] %>
      <% else %>
        <a class="nav-link <%= active == 3 ? 'active' : '' %>">3. Confirm</a>
      <% end %>
    </li>
  </ul>
</nav>
